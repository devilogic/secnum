2014.5.4 发现的问题：
--------------------------------------------------
问题一：
int SecNum::div(SecNum& v, SecNum& r) {
    if (lt(v)) {
        r.set(_secnum);
        zero();
        return 0;
    }

    unsigned q = 0;

q = 1 倍数会多1

问题二：
	自身处以自身，会多一个位
        分母为0会循环

--------------------------------------------------